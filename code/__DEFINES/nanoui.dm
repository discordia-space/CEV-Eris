// Argument passed to the force_open parameter on things such as nano_ui_interact.
#define NANOUI_UPDATE       0 // Simply update the UI.
#define NANOUI_REINITIALIZE 1 // Re-initialize the UI without closing the window.
#define NANOUI_FOCUS        2 // Update the UI and focus it to move it to top.
#define NANOUI_FORCE_OPEN   3 // Forcibly close & re-open the UI.
