<div class="display">
	<header><h3>Loaded materials</h3></header>

	<article>
	{{for data.materials}}
		<section>
			<span class="label">{{:value.name.toTitleCase()}}</span>
			{{value.amount = Math.round(value.count * 10) / 10;}}
			<div class="content">{{:value.amount}}/{{:data.mat_capacity}}</div>
			<div class="content">{{:helper.link('', 'eject', {'eject_material' : value.name}, null, 'statusValue')}}</div>
		</section>
	{{empty}}
		<section>
			<span class="label">None loaded</span>
		</section>
	{{/for}}
		<section>
			<div class="content">{{:helper.link('INSERT MATERIALS', '', {'insert' : 1}, null, 'bigButton')}}</div>
		</section>
	</article>
</div>