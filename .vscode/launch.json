{
	"version": "0.2.0",
	"configurations": [
	  {
		"type": "byond",
		"request": "launch",
		"name": "Launch and Build TGUI and DM",
		"preLaunchTask": "TGUI Compile",
		"dmb": "${workspaceFolder}/${command:CurrentDMB}"
	  },
	]
  }
